# General
spring.application.name = Book101-API
website.url=https://localhost:8443
server.port=8443
server.compression.enabled=true

# DB Source
database=mysql

spring.cloud.gcp.sql.database-name=book101
spring.cloud.gcp.sql.instance-connection-name=book101-cloud:us-central1:book101db

# Initialize the database since the newly created Cloud SQL database has no tables. The following flag is for Spring Boot 2.5+.
spring.sql.init.mode=always  
spring.jpa.hibernate.ddl-auto=none

#Mail stuff
spring.mail.protocol=smtp
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=book101.srl@gmail.com
spring.mail.from=book101.srl@gmail.com
spring.mail.password=Book101psw2021
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# JWT token
token.jwtSecret= d3jcD@!p]
token.jwtExpirationMs= 86400000
token.jwt.algorithm=HMAC256


